using System;
using System.Collections.Generic;
using System.Text;

namespace AlgorithmsPractice.Strings_Arrays
{
    public class OneAwayClass
    {
        //public static bool OneAway(string str1, string str2)
        //{
        //    int diffCount = 0;
        //    if (str1.Length == str2.Length)
        //        for (int i = 0; i < str1.Length && diffCount <= 1; i++)
        //            if (str1[i] != str2[i])
        //                diffCount++;

        //    else if(str1.Length - str2.Length == 1)

            
        //}

        //private static int GetDiffCount(string strBig, string strSmall)
        //{
        //    for (int i = 0; i < strBig.Length; i++)
        //    {
        //        if (strBig[i] != str2[i])
        //    }
        //}
    }
}
